# ── CODEOWNERS ───────────────────────────────────────
# Global fallback owner for the Muninn repository. The per-integration owner
# lines inherited from upstream Homarr were removed: those handles are not in
# the Emkraan org, so they only produced review requests that never resolved.
* @cmarko89
